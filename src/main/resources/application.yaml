"%prod":
  kafka:
    bootstrap:
      servers: "kafka:9092"

"%test":
  quarkus:
    hibernate-orm:
      database:
        generation: "drop-and-create"
    datasource:
      db-kind: "sqlite"
      username: "template"
      password: "secret"
      jdbc:
        url: "jdbc:sqlite:test.db"

quarkus:
  swagger-ui:
    always-include: true
  kafka:
    devservices:
      topic-partitions:
        test: 1
        test-acknowledged: 1
  hibernate-orm:
    database:
      generation: "update"
  datasource:
    db-kind: "sqlite"
    username: "template"
    password: "secret"
    jdbc:
      url: "jdbc:sqlite:database.db"

mp:
  messaging:
    outgoing:
      test-out:
        topic: "test"
      test-acknowledged-out:
        topic: "test-acknowledged"
      test-entity-out:
        topic: "test-entity"
    incoming:
      test-in:
        topic: "test"
      test-entity-in:
        topic: "test-entity"
